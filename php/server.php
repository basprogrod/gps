<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

<?php
echo '123';

// define('PORT', 8090)

// require_once ("classes/Chat.php");

// $chat = new Chat();

// $socket = socket_create(AF_INET, SOCK_STREAM, SOL_TCP);//создаём сокет

// socket_set_option($socket, SOL_SOCKET, SO_REUSEADDR, 1);//разрешаем использовать один порт для нескольких соединений

// socket_bind($socket, 0, PORT);//привязываем его к указанным ip и порту


// socket_listen($socket);//слушаем сокет

// // while (true) {

// //     $newSocket = socket_accept($socket);
// //     $header = socket_read($newSocket, 1024);
// //     Schat->sendHeaders($header, $newSocket, 'ig-v.ru/chat', PORT );
// // }

// ;
?>
    
</body>
</html>